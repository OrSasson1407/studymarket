/**
 * StudyMarket Mobile — React Native entry point
 *
 * Architecture:
 *   - Shares @studymarket/types and @studymarket/utils from the monorepo
 *   - Communicates with the same backend services as the web app via REST
 *   - Navigation: React Navigation (stack + bottom tabs)
 */
export { default } from './App';
